name: entity-patcher
run-name: entity-patcher unit tests
on: [push]
jobs:
  unit-tests:
    runs-on: ubuntu-latest
    steps:
      - run: composer install
      - run: vendor/bin/phpunit --coverage-clover=coverage.xml
      - run: bash <(curl -s https://codecov.io/bash)